# tttool.exe in TipToiPdf Ordner kopieren (wird nicht versioniert) und zu PATH hinzufügen
# MuseScore3.exe in musescore/bin zu PATH hinzufügen
# Musescore
- Schlagzeugstur als Metronom anlegen

TODO
tiptoi-Tool/david-maya anlegen, falls nicht existiert
config.json DIST
project.json DIST
01-export Benennung
.ogg-Erstellung schneller?
createAudio paralellisieren
Yaml files entfernen (nur gme und pdf behalten)
shell_exec async
shell_exec ohne quotes, wenn nicht noetig
cleanDir glob zusammenfassen
volume 100 fuer INstrumente 78 fuer Percussion?